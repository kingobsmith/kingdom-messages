import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Apply | Kingdom Messages",
  description: "Apply for Kingdom Chamber membership or request a private Kingdom Message invitation.",
};

export default function ApplyLayout({ children }: { children: React.ReactNode }) {
  return children;
}
